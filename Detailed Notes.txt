Task List:: 

-   Need to create architecture (Folder structure)
-   Start working on UI portion 
        -   Create Main Layout
        -   Landing screen
        -   Login screen
        -   Sign up screen 

-   Integrate SEO 
-   Integrate GA (Google Analytics)
        -   require client's email address so that we can configure it directly on that. 

-   Integrate login/sign up functionality
        -   Need to discuss with Backend developer how backend will works for authentication? 
        -   Login with google
        -   Login with Linkedin
        
        -   Login with email & password
        -   Sign up with email, password, re-password

-   Survey
        -   For survey need to prepare JSON object for display dynamically questions with their associated options. 
        -   Integrate api

----------------------------------------------------------------------------------------------------------------

Require APIs List with endpoints and payload

Note :: 
    - All APIs should return appropriate status code. (200, 400, 401, 403, 404 etc. )
    - All APIs should return response in below format 
        Example: 
        {
            data: null, // [], {}
            isSuccess: true,    // in every request (If Success)
            successMessage:"",  // in every request (If Success)

            isError: true,      // IF ERROR 
            errorMessage: "",   // IF ERROR
        }

APIs List
-   Get all static data (authentication not require)
    Method      ::      GET
    Endpoint    ::     /getStatic
    response    ::     
    {
        navBarOptions: [
            {
                name: "Demo",
            },
            {
                name: "Products",
                isSubOptions: true,
                subOptions: [
                    {
                        sub_name: "Media Sharing",
                        isChildAvail: true,
                        children: [
                            {

                            }
                        ]
                    }
                ]
            },
        ]
    }


-   Get all categories (authentication not require)
    Method      ::      GET
    Endpoint    ::     /getCategories
    response    ::     
    {
        totalCategories: 5, // Length of categories
        categories: [{
            category_name: "Social Network",
            uId: "", // RANDOM_GENERATED_UNIQUE_ID
            totalSubCategories: 10, // length of total categories which is associated with the social Network 
            subCategories: [{
                sub_uID: "", // RANDOM_GENERATED_UNIQUE_ID
                name: "",
                description: "",
                imageURL: "", // Need S3 link 
            }]
        }]    
    }



-   Get all survey questions (authentication not require)
    Method      ::      GET
    Endpoint    ::     /getCategories
    response    ::     
    {
        totalQuestions: 5, // Length of questions
        questions: {
            question_name: "What is your name?",
            uId: "", // RANDOM_GENERATED_UNIQUE_ID
            question_type: "text", // question_type should be (text, radio, multiselect, boolean/switch)
        },

    }

